name: Build .NET e Rodar Docker Compose

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Configurar o SDK do .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restaurar dependências e Build da Solução
      run: dotnet build EduOnline.slnx --configuration Release

    # - name: Configurar o JDK (necessário para o Sonar Scanner)
    #   uses: actions/setup-java@v4
    #   with:
    #     distribution: 'zulu'
    #     java-version: '17'

    # - name: Instalar o Sonar Scanner para .NET
    #   run: dotnet tool install --global dotnet-sonarscanner

    # - name: Iniciar análise do SonarCloud
    #   env:
    #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   run: >
    #     dotnet sonarscanner begin
    #     /k:"eduonline-platform"
    #     /o:"${{ secrets.SONAR_ORGANIZATION }}"
    #     /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
    #     /d:sonar.host.url="https://sonarcloud.io"

    # - name: Executar Testes de Unidade
    #   run: >
    #     dotnet test EduOnline.slnx --configuration Release --no-build
    #     --filter "FullyQualifiedName!~IntegrationTest"
    #     /p:CollectCoverage=true
    #     /p:CoverletOutputFormat=opencover
    #     /p:CoverletOutput="../../coverage/"
    #     /p:MergeWith="../../coverage/coverage.opencover.xml"

    # - name: Subir RabbitMQ para os testes de integração
    #   run: |
    #     docker run -d --name rabbitmq-test \
    #       -p 5672:5672 \
    #       rabbitmq:3-management-alpine

    # - name: Subir EventStore para os testes de integração
    #   run: |
    #     docker run -d --name eventstore-test \
    #       -e "EVENTSTORE_INSECURE=true" \
    #       -e "EVENTSTORE_RUN_PROJECTIONS=All" \
    #       -e "EVENTSTORE_START_STANDARD_PROJECTIONS=true" \
    #       -e "EVENTSTORE_ENABLE_ATOM_PUB_OVER_HTTP=true" \
    #       -p 2113:2113 \
    #       eventstore/eventstore:24.10.0

    # - name: Aguardar RabbitMQ e EventStore ficarem disponíveis
    #   run: |
    #     for i in $(seq 1 30); do
    #       if docker exec rabbitmq-test rabbitmq-diagnostics -q ping > /dev/null 2>&1; then
    #         echo "RabbitMQ disponível"
    #         break
    #       fi
    #       echo "Aguardando RabbitMQ... ($i/30)"
    #       sleep 5
    #     done

    #     for i in $(seq 1 30); do
    #       if curl -sf http://localhost:2113/health/live > /dev/null 2>&1; then
    #         echo "EventStore disponível"
    #         break
    #       fi
    #       echo "Aguardando EventStore... ($i/30)"
    #       sleep 5
    #     done

    - name: Executar Testes de Integração
      # Validação pontual: rodando apenas o projeto Pagamentos.IntegrationTest
      run: >
        dotnet test test/EduOnline.Alunos.IntegrationTest/EduOnline.Alunos.IntegrationTest.csproj
        /p:CollectCoverage=true
        /p:CoverletOutputFormat=opencover
        /p:CoverletOutput="../../coverage/"
        /p:MergeWith="../../coverage/coverage.opencover.xml"

    # - name: Remover RabbitMQ e EventStore de testes
    #   if: always()
    #   run: |
    #     docker rm -f rabbitmq-test eventstore-test
    #     docker rmi rabbitmq:3-management-alpine eventstore/eventstore:24.10.0

    # - name: Finalizar análise do SonarCloud
    #   env:
    #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   run: dotnet sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"

    - name: Fazer Login no Docker Hub
      if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Publicar imagens no Docker Hub
      if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
      run: docker compose push

  docker-compose:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Fazer Login no Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Baixar as imagens publicadas no Docker Hub
      run: docker compose pull

    - name: Subir o Docker Compose completo
      run: docker compose up -d

    - name: Verificar status dos containers
      run: docker compose ps
