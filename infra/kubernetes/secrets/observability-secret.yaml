apiVersion: v1
kind: Secret
metadata:
  name: eduonline-observability-secret
  namespace: eduonline
type: Opaque
stringData:
  # Example placeholder for collector API key or other sensitive settings.
  # Replace with real values in production and do not commit secrets to the repo.
  OTEL_COLLECTOR_API_KEY: "<replace-with-secure-key>"
