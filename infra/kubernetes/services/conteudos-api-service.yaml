apiVersion: v1
kind: Service
metadata:
  name: conteudos-api
  namespace: eduonline
spec:
  type: ClusterIP
  selector:
    app: conteudos-api
  ports:
    - name: http
      port: 5001
      targetPort: 5001
